#include <stdio.h>
#include <assert.h>

/**
 * INSTRUCCIONES
 * 
 * Completa el siguiente programa, para que calcule la sumatoria de los elementos
 * de mi_arreglo
 * 
 * usa un ciclo for.
 */

int main()
{
    printf("--[ Sumatoria de arreglo ]--\n");
    int mi_arreglo[10] = {2, 5, 4, 3, 7, 9, 6, 4, 2, 0};
    int sum;

    // escribe aqui tu ciclo for

    assert(sum == 42);

    printf("Ejercicio terminado correctamente!\n");
    return 0;
}