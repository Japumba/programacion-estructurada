#include <stdio.h>

/**
 * INSTRUCCIONES
 * 
 * El siguiente código declara un arreglo con 10 elementos, y luego imprime una
 * linea con ese arreglo, con un formato especial.
 * 
 * Escribe un ciclo for y cuantos printfs necesites, para que se imprima
 * en la pantalla el arreglo tal cual se pide.
 * 
 * todos los printf() deben etar >>DENTRO<< del for
 *  puedes usar condicionales dentro del for ;)
 * 
 * DEBE SER IDENTICO
 */

int main()
{
    int mi_arr[10] = {1, 8, 320, 4, -1, 6, 4, 2, 10, 5};
    printf("Las siguientes dos líneas de la consola deben ser identicas:\n");
    printf("arr: [1, 8, 320, 4, -1, 6, 4, 2, 10, 5]\n");

    // Escribe aqui tus printfs y tu ciclo for.

    printf("\n");
    return 0;
}